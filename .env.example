# 32+ char secret used to seal the session cookie
NUXT_SESSION_PASSWORD=
# shared password guests enter at the gate
NUXT_SITE_PASSWORD=
# libSQL database: blank = local file ./.data/wedding.db; production = Turso URL + token
NUXT_DB_URL=
NUXT_DB_AUTH_TOKEN=
