## 1. A6 landscape letters, two per sheet (Option C)

- [x] 1.1 Install `print-materials.feature` into `tests/features/` (overwriting). In `tests/steps/print-materials.steps.ts`: delete the "Decorative back page" and "Letter back floral art" rules; rewrite "Batch letter printing" (sheets `[data-print-page]` = ⌈parties ÷ 2⌉; `[data-letter]` = parties, each with `[data-qr] svg` and its own `?t=` in `[data-fallback]`); add "Two letters per A5 sheet" (every sheet `print-page-a5`, ≤ 2 `[data-letter]` per sheet, `[data-cut]` per sheet, no `[data-crop]`, no `[data-letter-back]`); add "Two letters per A4 sheet with crop marks" (`routeState.query = { sheet: 'a4' }`, every sheet `print-page-a4`, ≤ 2 letters, `[data-crop]` present, `[data-letter]` inner HTML identical to the A5 render for the same party); add "Sheet toggle is screen-only" (`[data-sheet-toggle]` exists with buttons labelled A5 and A4 and sits inside a `.no-print` ancestor); add "Single-party reprint" (1 sheet, 1 letter via `routeState.query = { party }`); add "Names match the site header style" (`[data-letter] h1` classes include `font-display`, `font-light`, `italic`, contains `span.text-petal` with `&`, and next element is `[data-letter-header-divider]` with `mx-auto`); rewrite "Letter shows a closing divider" to a DOM check (`[data-letter] [data-letter-divider]` per letter); extend "Letters carry tulip corners" to also assert `FloralCluster` in the page source. Run `npm test` — confirm the changed letter scenarios fail (red).
- [x] 1.2 Re-layout `letters.vue` per canvas Option C: `sheet = ref(route.query.sheet === 'a4' ? 'a4' : 'a5')`; a `no-print` bar with a `[data-sheet-toggle]` two-button segmented control (`aria-pressed`) above the sheets; chunk `letters` into pairs; per pair a `<PrintPage :size="sheet" style="--page-pad: 0">` whose block (`w-[148mm]`, two stacked `[data-letter]` slots `relative overflow-hidden w-[148mm] h-[105mm]`, second slot empty on an odd count) is flush on A5 with a `[data-cut]` dashed hairline between the slots, and on A4 is `mx-auto mt-[43.5mm]` with six `[data-crop]` corner sets (four 5 mm × 1px `bg-ink/50` ticks each, offset 1.5 mm outside the trim, positioned in mm from the same offsets) in place of the dashed line. Each slot: `aria-hidden` bleed layer (`FloralCluster` top-left `w-40 -translate-x-1/2 -translate-y-1/2 rotate-[135deg]`, `FloralTulipCorner` bottom-right `w-16 -scale-x-100`, `FloralDivider` `data-letter-divider` `w-28` centred `bottom-4`) and a `p-[8mm] grid grid-cols-2 gap-5 items-center h-full` body. Left column: centred stack of eyebrow, `<h1 class="font-display text-4xl font-light italic text-ink">Ciera <span class="text-petal">&amp;</span> Martin</h1>`, `FloralDivider data-letter-header-divider class="mx-auto mt-1 w-28"`; then left-aligned greeting and copy. Right column centred: QR `35mm`, "Scan to RSVP, or visit", fallback URL. Remove `<PrintLetterBack />` and `FloralHeader`. No type-size changes.
- [x] 1.3 Delete `app/components/PrintLetterBack.vue`; update `app/pages/admin/print/index.vue` description to "A6 invite letters, two per A5 or A4 sheet — print at 100%". `npm test` green.
- [x] 1.4 Open `/admin/print/letters` in the dev server with ≥ 3 parties; on each sheet setting print to PDF at 100% with no pages-per-sheet option: confirm ⌈n ÷ 2⌉ pages of the chosen size, each matching its on-screen sheet, everything inside each A6 letter, A4 crop marks outside the trim, type sizes unchanged (the `@manual` scenarios).
- [x] 1.5 Quality pass: refactor if needed, then `npm test`, `npm run lint`, `npm run typecheck` all green.
