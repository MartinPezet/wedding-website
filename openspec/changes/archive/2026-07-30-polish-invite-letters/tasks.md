## 1. Invite letter QR styling and bottom divider

- [x] 1.1 Install `print-materials.feature` into `tests/features/` (overwrite), write/extend step definitions for the "QR code matches letter styling" and "Letter shows a closing divider" scenarios, plus a unit test asserting `qrSvg()`'s output has a transparent background, petal-deep-filled data modules, rounded data-module rects, and sharp finder-square rects — confirm red.
- [x] 1.2 Rewrite `qrSvg()` in `shared/utils/qr.ts` to use `QRCode.create(text, opts)`'s raw module matrix instead of the library's `toString()` SVG renderer: emit one `<rect>` per dark module (transparent background, `fill` set to `--color-petal-deep`'s hex), rounding data-module rects (`rx`/`ry`) while the three 7×7 finder-pattern corner regions stay sharp-cornered.
- [x] 1.3 Update `app/pages/admin/print/letters.vue`: remove the now-dead `text-ink` class on the `data-qr` wrapper div; add a centered `FloralDivider` at the bottom of each letter. (Shipped differently than planned: `mt-auto` inside `<article>` turned out to be a no-op — `min-h-full` there resolves against `.print-page-body`, which has no definite height of its own, so the flex chain never reached real page height. Fixed by moving the divider into the `#bleed` slot instead, `absolute bottom-12 right-1/2 translate-x-1/2`, matching how the tulip corners are already anchored there — `.print-page-bleed` sizes correctly off `.print-page`'s real `min-height` directly, sidestepping the broken chain entirely. Verified in-browser: divider sits exactly 48px above the true page edge, centered.)
- [x] 1.4 Quality pass: `npm test`, `npm run lint`, `npm run typecheck` green.

## 2. Party token format

- [x] 2.1 Install `site-gate.feature` into `tests/features/` (overwrite), write/extend step definitions for "Token is hand-transcribable" and "Token matches regardless of letter case", plus a unit test on `generatePartyToken()` asserting exactly 10 characters from the Crockford alphabet (`0-9`, `A-Z` excluding `I`, `L`, `O`, `U`) — confirm red.
- [x] 2.2 Replace `generatePartyToken()` in `server/utils/token.ts`: draw 10 characters from the Crockford base32 alphabet using a rejection-sampled random source (e.g. `crypto.randomInt` or discarding out-of-range bytes from `randomBytes`) to avoid modulo bias.
- [x] 2.3 Make token lookup in `server/middleware/token-gate.ts` case-insensitive (uppercase the incoming `?t=` value before the DB comparison); confirm generated/stored tokens are uppercase.
- [x] 2.4 Add a bounded retry-on-unique-conflict loop in `server/utils/parties.ts#createParty` when inserting a freshly generated token, regenerating and retrying on a unique-constraint violation. (Added `tests/unit/parties.test.ts` with a mocked-collision test to confirm red before implementing.)
- [x] 2.5 Regenerate tokens for all existing parties (via the existing per-party admin regenerate endpoint, `server/api/admin/parties/[id]/token.post.ts`) once this ships, and reprint/redistribute any invite letters — old tokens stop working immediately. (Operational step, not code — documented in design.md's Migration Plan. Confirmed with the user: no invite codes have been generated in production yet, so nothing to regenerate there. Local dev DB has 46 old-format token rows — all disposable dev/test fixtures ("Sample Family," "Tester Family," 43 synthetic "Seating Party NN" rows) — which now fail to match under the new case-insensitive `token-gate.ts` lookup (old tokens are mixed-case base64url, lookup uppercases before comparing). Confirmed acceptable, no fix needed; dev can reseed/regenerate locally if it matters.)
- [x] 2.6 Quality pass: `npm test`, `npm run lint`, `npm run typecheck` green. (Along the way, found `guest-data`'s "Secure party tokens" requirement — the actual owner of token generation format — was missing from the proposal's Modified Capabilities; corrected proposal.md, moved the format requirement from `site-gate` to a new `specs/guest-data/spec.md` delta, added `features/guest-data.feature`, and updated `tests/steps/guest-data.steps.ts` to match.)
